<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 音声コマンド（ネイティブの音声認識） -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- AI 写真の撮影（WebView の file input capture） -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- 荷降ろし中をステータスバーに出しておくため -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Android 11+ で音声認識サービス / TTS エンジンを見つけるための宣言 -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
        <!-- 「写真を撮って読込」でカメラアプリを探すため -->
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ContainerSync"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|uiMode"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- カメラで撮った写真を WebView に渡すための入れ物 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- 裏に回ったまま 20 分たったらアプリを終了する -->
        <receiver
            android:name=".work.IdleShutdownReceiver"
            android:exported="false" />

        <!-- 荷降ろし中のあいだ、アプリを閉じてもステータスバーに表示を残す -->
        <service
            android:name=".work.WorkStatusService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>

</manifest>
