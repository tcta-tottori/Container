<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- UI は Compose Material 3 で描くので、Activity のテーマは最小限にする -->
    <style name="Theme.ContainerSync" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
